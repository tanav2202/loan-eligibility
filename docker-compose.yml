services:
  analysis:
    image: tanav2202/loan-eligibility:latest
    ports:
      - "8888:8888"
    volumes:
      - .:/home/jovyan/work
    working_dir: /home/jovyan/work
    deploy:
      resources:
        limits:
          memory: 5G
    platform: linux/amd64
    command: start-notebook.sh --NotebookApp.token='' --NotebookApp.password=''